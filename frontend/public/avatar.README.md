# Avatar placeholder

Drop a 256×256 (or larger) square photo named **`avatar.png`** in this folder
before going live. The header references `/avatar.png`; if the file is
missing the UI hides the image gracefully instead of showing a broken icon.
